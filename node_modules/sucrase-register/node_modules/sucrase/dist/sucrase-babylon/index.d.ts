import { Token } from "./tokenizer";
import { Scope } from "./tokenizer/state";
export declare type File = {
    tokens: Array<Token>;
    scopes: Array<Scope>;
};
export declare function parse(input: string, plugins: Array<string>): File;
